# 🔗 智慧媽咪短網址系統

自建短網址管理系統，取代 Lihi 付費服務。

## 功能

- ✅ 多網域 / 子網域支援
- ✅ 短網址建立，目標隨時更換
- ✅ 路徑參數自由擴展（/slug/FB/IG/任意文字）
- ✅ A/B 分流測試（按權重分配流量）
- ✅ QR Code 產生器（可嵌入品牌 Logo）
- ✅ Link in Bio 頁面（完全客製化）
- ✅ 後台統一管理介面

## 技術架構

- **前端/後端**：Next.js 15 (App Router)
- **資料庫**：Supabase (PostgreSQL)
- **部署**：Vercel
- **樣式**：Tailwind CSS

## 快速開始

### 1. 建立 Supabase 專案

前往 [supabase.com](https://supabase.com) 建立專案，然後到 SQL Editor 執行：

```
supabase/migrations/001_init.sql
```

### 2. 設定環境變數

```bash
cp .env.example .env.local
```

填入 Supabase 的 URL、Key，以及管理員密碼。

### 3. 安裝並啟動

```bash
npm install
npm run dev
```

### 4. 前往後台

瀏覽 `http://localhost:3000/admin`，輸入管理員密碼登入。

## 部署到 Vercel

1. 推送到 GitHub
2. 在 Vercel import 專案
3. 設定環境變數
4. 到 Settings → Domains 新增你的網域/子網域

### DNS 設定

```
類型    主機名稱    指向
CNAME   go          cname.vercel-dns.com
CNAME   link        cname.vercel-dns.com
```

## 使用方式

### 短網址

```
https://go.smartmommy.com/母親節
https://go.smartmommy.com/母親節/FB
https://go.smartmommy.com/母親節/IG/限動

→ 全部導向你設定的目標網址
```

### Bio 頁面

```
https://go.smartmommy.com/@links
```

### QR Code

在後台的短網址管理中，點擊「QR」按鈕即可設定與下載。

## 專案結構

```
src/
├── app/
│   ├── [...slug]/route.ts       # 短網址重定向 + Bio 頁面渲染
│   ├── admin/                    # 後台管理介面
│   │   ├── layout.tsx           # 管理介面框架 + 登入
│   │   ├── page.tsx             # 總覽
│   │   ├── domains/page.tsx     # 網域管理
│   │   ├── links/page.tsx       # 短網址管理 + QR Code
│   │   └── bio/page.tsx         # Bio 頁面管理
│   ├── api/                      # API 路由
│   │   ├── auth/                # 認證
│   │   ├── domains/             # 網域 CRUD
│   │   ├── links/               # 短網址 CRUD + 目標管理
│   │   ├── bio/                 # Bio 頁面 CRUD + 連結管理
│   │   └── qr/                  # QR Code 產生
│   ├── layout.tsx
│   └── page.tsx
├── lib/
│   ├── supabase.ts              # Supabase 客戶端
│   ├── types.ts                 # TypeScript 型別
│   ├── utils.ts                 # 工具函式
│   └── auth.ts                  # 認證邏輯
└── supabase/
    └── migrations/001_init.sql  # 資料庫 Schema
```
